const { userRouter } = require('./user.router');
const { productsRouter } = require('./products.router');
const { saleRouter } = require('./sale.router');

module.exports = { userRouter, productsRouter, saleRouter };
